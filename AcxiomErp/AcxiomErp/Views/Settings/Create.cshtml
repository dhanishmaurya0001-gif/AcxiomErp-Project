@model AcxiomErp.Models.Setting

@{
    ViewData["Title"] = "Add Setting";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="~/css/settings.css" />

<div class="container mt-5">
    <h2 class="mb-4">➕ Add New Setting</h2>

    <div class="card shadow-sm">
        <div class="card-body">

            <form asp-action="Create" method="post">

                <div class="mb-3">
                    <label class="form-label">Category</label>
                    <select class="form-select" asp-for="Category" required>
                        <option value="">-- Select Category --</option>
                        <option>Application</option>
                        <option>Security</option>
                        <option>Modules</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label">Key</label>
                    <input asp-for="Key" class="form-control" placeholder="Example: CompanyName" required />
                </div>

                <div class="mb-3">
                    <label class="form-label">Value</label>
                    <input asp-for="Value" class="form-control" placeholder="Example: Acxiom ERP" required />
                </div>

                <button type="submit" class="btn btn-success">Save</button>
                <a asp-action="Index" class="btn btn-secondary ms-2">Cancel</a>

            </form>

        </div>
    </div>
</div>
